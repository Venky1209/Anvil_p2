# Core (stdlib only — no external deps for ingestion layer)
# SQLite is used as the storage backend (stdlib)

# Optional — swap SQLite for DuckDB when available for better analytical perf
# duckdb>=0.10.0

# Used by memory module for graph traversal in deep mode
networkx>=3.2

# Used by memory module for fingerprint vectors and cosine similarity
numpy>=1.24.0
